<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil</title>
    <link type="text/css" rel="stylesheet" href="/css/index.css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-4">
    <h1>Perfil</h1>
    
    <div class="card mt-4">
        <div class="card-body">
            <p><strong>Email:</strong> {{usuario.email}}</p>
            <p><strong>Fecha de Creación:</strong> {{usuario.createdAt}}</p>
            <p><strong>Última Actualización:</strong> {{usuario.updatedAt}}</p>
        </div>
    </div>

    <div class="mt-3">
        <button type="button" class="btn btn-primary IndexGestion">Gestion</button>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/indexGestion.js"></script>
</body>
</html>
