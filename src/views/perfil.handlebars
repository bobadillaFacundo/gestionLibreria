<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link type="text/css" rel="stylesheet" href="/css/index.css">
</head>

<body class="container mt-5">
    <h1 class="text-center">Perfil</h1>
    <div class="card p-4 shadow-sm">
        <p><strong>Email:</strong> {{usuario.email}}</p>
        <p><strong>Fecha de Creación:</strong> {{usuario.createdAt}}</p>
        <p><strong>Última Actualización:</strong> {{usuario.updatedAt}}</p>
    </div>
    <div class="text-center mt-3">
        <button type="button" class="btn btn-primary IndexLibros">Libros</button>
        <button type="button" class="btn btn-primary Indexcompras">Compras</button>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/indexLibro.js"></script>
</body>

</html>