<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link type="text/css" rel="stylesheet" href="/css/index.css">
</head>

<header class="container mt-3">
    <button class="btn btn-primary btn-volver-carrito">Volver</button>
</header>

<body>
    <!-- Contenedor principal -->
    <div class="container mt-4">
        <h1 class="text-center">Lista de Libros en tu Carrito</h1>
<section class="libros-section">
        <div class="d-flex justify-content-center gap-2 mb-4">
            <button class="btn btn-secondary btn-registrar-compra" data-id-carrito="{{carrito._id}}">Comprar</button>
      </div>
        </section>

        <!-- Sección de Libros -->
        <section class="libros-section">
            {{#each carrito.libros}}
            <div class="card shadow-sm mb-3">
                <div class="card-body">
                    <h5 class="card-title">Libro</h5>
                    <p><strong>ID:</strong> {{_id}}</p>
                    <p><strong>Autor:</strong> {{autor.nombre}}</p>
                    <p><strong>Título:</strong> {{titulo}}</p>
                    <p><strong>Descripción:</strong> {{descripcion}}</p>
                    <p><strong>Precio:</strong> {{precio}}</p>
                    <p><strong>Cantidad:</strong> {{cantidad}}</p>

                    <h6>Lista de Categorías:</h6>
                    <div class="list-group">
                        {{#each categorias}}
                        <div class="list-group-item">
                            <p>{{nombre}}</p>
                        </div>
                        {{/each}}
                    </div>
                </div>
            </div>
            {{/each}}
        </section>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/carritoindex.js"></script>
</body>

</html>